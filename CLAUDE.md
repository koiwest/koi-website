# koi-website - Functional Website Reconstruction Skill
Agent skill + phase files + references for evidence-first product cloning, incremental feature implementation, sub-agent research, and AI curation systems.

<directory>
phases/ - Executable phase protocols (5 files: recon, architecture, mock slice, provider, polish)
</directory>

<directory>
references/ - Supporting doctrine and domain protocols (5 files: doctrine, sub-agent protocol, curation MVP, judgment protocol, validation)
</directory>

<config>
SKILL.md - Trigger surface, phase router, hard rules, and resource routing.
CLAUDE.md - L1 architecture map for the skill itself.
</config>

Architecture decision: keep `SKILL.md` as a router; put website construction steps in `phases/`; keep reusable doctrine and product-specific protocols in `references/`.

Change log:
- 2026-07-03: Created `/koi-website` as a function-first website reconstruction skill.
- 2026-07-03: Promoted the skill from reference navigation to SL-name-style phase execution with sub-agent protocol.

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
